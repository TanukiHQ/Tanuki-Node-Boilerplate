{
    "app": {
        "name": "Tanuki Boilerplate",
        "secret": "secretKey"
    },
    "webserver": {
        "webAddress": "localhost:5000",
        "maxRequestsPerMinute": 100,
        "port": 5000
    },
    "services": {
        "mongo": {
            "host": "mongodb://localhost:27017",
            "database": "tanukiDb",
            "options": "?readPreference=primary&directConnection=true&ssl=true"
        }
    }
}